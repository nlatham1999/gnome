
// import { useUser } from '@auth0/nextjs-auth0';

export function addNewProject(data) {
    // const { user, error } = useUser();

    // if(error || !user) {
        return;
    // }

    //we have the user now add it to the database

    //add data 
}